crawlers:
  news:
    schedule_minutes: 15
    cutoff_minutes: 30
    source_timeout_seconds: 10
    retry_count: 3
    retry_delay_seconds: 10
  weather:
    schedule_minutes: 30
    source_timeout_seconds: 15
  stock:
    sync_interval_minutes: 60
    analyze_schedule: "0 6 * * 1-5"   # 평일 06:00 (KST)

api:
  default_minutes: 30
  max_limit: 100

retention:
  days: 30
